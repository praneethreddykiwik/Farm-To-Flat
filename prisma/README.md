# prisma — schema and migrations (Adnan)

Placeholder. Seventeen tables per TDD §03. Money as BigInt paise, quantities as Decimal(10,3), TIMESTAMPTZ everywhere.
Remember both DATABASE_URL (pooler, 6543) and DIRECT_URL (5432) in the datasource block.
