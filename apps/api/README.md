# apps/api — Express API (Tharun: platform · Adnan: money)

Placeholder. Owned by the platform and money streams. The mobile app expects the contract in
`docs/api-contract.md` at `/api/v1`. Bind to `0.0.0.0` so phones on the LAN can reach it.
