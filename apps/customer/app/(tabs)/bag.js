import React from 'react';
import { View } from 'react-native';

/** Placeholder route: the tab press is intercepted in the tabs layout and opens the /cart sheet. */
export default function BagTab() {
  return <View />;
}
