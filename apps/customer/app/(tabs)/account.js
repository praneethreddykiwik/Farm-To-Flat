import React from 'react';
import { View } from 'react-native';

/** Placeholder route: the tab press is intercepted in the tabs layout and opens the /profile screen
 *  (named "account" here so it doesn't collide with the real app/profile.js route). */
export default function AccountTab() {
  return <View />;
}
