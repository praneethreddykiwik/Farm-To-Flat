node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
# Don't reinstall dependencies on every `pnpm <script>` run — it breaks git hooks and CI shells.
verify-deps-before-run=false
